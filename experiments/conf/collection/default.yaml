_target_: shortcut_learning.configs.CollectionConfig
seed: 42
max_shortcuts_per_graph: 100
use_random_rollouts: False
num_rollouts_per_node: 50
max_steps_per_rollout: 50
shortcut_success_threshold: 1
action_scale: 1.0
collect_episodes: 10

# V2 collection parameters
states_per_node: 10  # Number of diverse states to collect per node
perturbation_steps: 5  # Random action steps for state diversity

skip_collect: False
