seed: 0

defaults:
  - _self_
  - approach: random  # Change to pure_rl to use PureRLApproach
  - system: obstacle2d
  - collection: default
  - training: default
  - evaluation: default
  - policy: default
