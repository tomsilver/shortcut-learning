seed: 0

defaults:
  - _self_
  - approach: default  # Change to pure_rl to use PureRLApproach
  - system: obstacle2d
  - collection: default
  - training: default
  - evaluation: default
  - policy: default
